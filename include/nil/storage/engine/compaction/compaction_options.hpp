#ifndef STORAGE_ENGINE_COMPACTION_OPTIONS_HPP
#define STORAGE_ENGINE_COMPACTION_OPTIONS_HPP

namespace nil {
    namespace engine {
        struct compact_range_options {

        };

        struct compaction_options {

        };
    }
}

#endif    // DCDB_COMPACTION_OPTIONS_HPP
